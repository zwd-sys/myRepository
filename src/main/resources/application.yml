spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/diandan?characterEncoding=UTF-8
    username: root
    password: root
mybatis:
  mapper-locations: classpath:sql/Mapper/*.xml
  type-aliases-package: com.cdtu.model
logging:
  level:
    sql: trace
    ROOT: info
server:
  port: 8079